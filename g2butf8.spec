# -*- mode: python -*-
a = Analysis(['g2butf8.py'],
             pathex=['/Users/tenyi/Documents/git/chinese-autoconvert'],
             hiddenimports=[],
             hookspath=None,
             runtime_hooks=None)
pyz = PYZ(a.pure)
exe = EXE(pyz,
          a.scripts,
          exclude_binaries=True,
          name='g2butf8',
          debug=False,
          strip=None,
          upx=True,
          console=True )
coll = COLLECT(exe,
               a.binaries,
               a.zipfiles,
               a.datas,
               strip=None,
               upx=True,
               name='g2butf8')
